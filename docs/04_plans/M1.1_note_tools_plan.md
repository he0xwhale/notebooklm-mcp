# Plan M1.1: Note Tools Implementation Plan

## 1. Scope

Register and implement `note_list` and `note_get` MCP tools and corresponding HTTP endpoints.

## 2. Implementation Steps

1. Add new TypeScript interfaces to `src/types.ts` (Done).
2. Register canonical/legacy names in `src/tools/tool-names.ts` (Done).
3. Implement core scraping and automation methods in `src/content/content-manager.ts` (Done).
4. Register the new tool schemas and tool handler actions in `src/tools/index.ts` (Done).
5. Expose tool actions in the MCP dispatcher in `src/index.ts` (Done).
6. Expose REST endpoints `/content/notes/list` and `/content/notes/get` in `src/http-wrapper.ts` (Done).
7. Execute build to verify TypeScript compilation.
8. Author a test script `src/tests/M1/NoteTools/verify_note_tools.ts` to run physical verification and generate evidence.
9. Compile test results and author a verification report.
